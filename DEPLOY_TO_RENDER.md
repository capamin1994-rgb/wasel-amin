# 🚀 كيفية رفع المشروع على منصة Render

## 1. ما هي منصة Render؟
هي منصة لاستضافة التطبيقات (مثل موقعك) على الإنترنت بسهولة. توفر خطة مجانية ممتازة للتجارب.

## 2. خطوات الرفع (Deployment)

### الخطوة الأولى: رفع الكود إلى GitHub
تتطلب Render أن يكون الكود الخاص بك موجوداً على GitHub (أو GitLab).
1. أنشئ مستودع (Repository) جديد على GitHub.
2. ارفع ملفات مشروعك إليه.

### الخطوة الثانية: التسجيل في Render
1. اذهب إلى [render.com](https://render.com).
2. سجل حساباً جديداً (يمكنك استخدام حساب GitHub الخاص بك).

### الخطوة الثالثة: إنشاء خدمة ويب (Web Service)
1. في لوحة تحكم Render، اضغط على **"New +"** ثم اختر **"Web Service"**.
2. اربط حساب GitHub الخاص بك واختر المستودع الذي رفعته.
3. ستكتشف Render تلقائياً إعدادات المشروع بفضل ملف `render.yaml` الذي أضفناه.
4. اضغط **"Create Web Service"**.

---

## ⚠️ ملاحظة هامة جداً (Persistent Data)
بما أن مشروعك يستخدم:
1. **SQLite Database** (ملف `database.sqlite`)
2. **WhatsApp Sessions** (ملفات الجلسات)

فإن الرفع على الخطة المجانية (Free Plan) في Render سيعني أن **البيانات ستُحذف عند كل إعادة تشغيل للخادم** (لأن الخطة المجانية لا تحتفظ بالملفات).

### الحل للإنتاج (Production):
للحفاظ على البيانات والجلسات بشكل دائم، تحتاج إلى:
1. **تفعيل Render Disk (مدفوع):** إضافة قرص تخزين دائم (Disk) وربطه بمسار المشروع.
2. **أو استخدام قاعدة بيانات خارجية:** مثل PostgreSQL (توفرها Render أيضاً) وتعديل الكود ليعمل معها بدلاً من SQLite.

## ✅ تم إصلاح مشكلة الصور
تم تحديث الكود لاستخدام روابط صور إسلامية موثوقة وثابتة بدلاً من الرابط الذي كان يسبب خطأ 404.
